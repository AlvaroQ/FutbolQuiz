<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginTop="16dp"
    android:layout_marginBottom="6dp"
    android:layout_marginStart="12dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/imageChampionship"
        android:layout_width="40dp"
        android:layout_height="25dp"
        tools:src="@drawable/flag_spain"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:contentDescription="@string/flag" />

    <TextView
        android:id="@+id/textChampionship"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Primera división"
        android:gravity="center"
        android:layout_marginStart="8dp"
        android:textAppearance="@style/Regular.TextTitleColor.Medium"
        app:layout_constraintTop_toBottomOf="@+id/imageChampionship"
        app:layout_constraintBottom_toTopOf="@+id/imageChampionship"
        app:layout_constraintStart_toEndOf="@+id/imageChampionship" />


</androidx.constraintlayout.widget.ConstraintLayout>